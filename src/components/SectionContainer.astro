---
import type { Props } from "@interfaces/SectionContainer";

const { className } = Astro.props;
---

<section class={`w-full md:w-[720px] container mx-auto ${className}`}>
  <slot />
</section>

<style></style>
